import Anthropic from '@anthropic-ai/sdk';
import type { GeneratedSummary, Topic } from './types';

const ANONYMIZATION_RULES = `
**절대 규칙 (위반 시 콘텐츠 폐기):**
- 보험사명 직접 언급 금지: 삼성생명/한화생명/DB손해보험/현대해상/메리츠화재 등 어떤 회사명도 X
  → "한 생명보험사", "주요 손해보험사", "업계", "일부 보험사" 로 익명화
- 상품명 직접 언급 금지: "○○플랜" 같은 구체 상품명 X → "실손의료보험", "암보험" 등 일반명사
- 광고 절대표현 금지: 최고/최저/100%/완벽/유일/절대/무조건/확실
- 비교표현 금지: "다른 보험사보다", "타사 대비", "업계 1위"
- 보장 단정 금지: "보장됩니다", "확실히", "평생 보장" → "약관에 따라", "조건 충족 시" 로 조건부
`;

// 카카오페이 머니콘텐츠 정밀 분석 후 도출한 패턴 — 이 구조 그대로 따라야 "예쁜 정리"가 나옴
const KAKAOPAY_STRUCTURE = `
**카카오페이 머니콘텐츠 정확한 구조 (반드시 따를 것):**

═══════════════════════════════════
**1. 도입부** — 3~4문장, 200~280자
   → 다음 둘 중 하나로 시작:
     (a) 시점/계기: "지난달 ○○가 발표됐어요. ..."
     (b) 공감 시나리오: "○○할 때, 한 번쯤 이런 고민 해보셨죠? ..."
   → 마지막 문장은 "○○에 대해 알아볼게요" / "어떻게 다른지 정리해드릴게요" 형태

═══════════════════════════════════
**2. 본문** — H2 섹션 정확히 **3~4개**

   각 섹션 규칙:
   - ## 헤딩은 평서문 또는 키워드 (예: "4세대보다 보험료를 덜 내요", "도수치료는 보장 못 받을 수 있어요")
   - 한 섹션 = 짧은 단락 2~4개 (한 단락 2~4문장, 150~350자)
   - **굵게**로 강조 (수치·용어·핵심 변화) — 섹션당 4~6회
   - 비교/단계가 있으면 **불릿 리스트** 또는 **표** 사용
   - 콜아웃은 본문에 **최대 1~2개만** (남용 금지). 형식:
     \`> 💡 [알아두세요]\` 핵심 정보
     \`> ⚠️ [주의]\` 함정 경고
     \`> 💬 [실제 사례]\` 가상 케이스 (이름 + 상황 + 결과 형식, 예: "40대 김○○ 씨는...")

═══════════════════════════════════
**3. 마무리** — 반드시 \`> 📌 [핵심 요약]\` 콜아웃 1개로 끝냄
   내부에 ✅ 또는 1./2./3. 체크리스트 형식 3~4개 항목.
   각 항목은 짧고 행동 지향 ("○○에서 ○○ 항목을 확인하세요" 형태).

   예시:
   > 📌 [핵심 요약]
   >
   > 1. 내 실손이 몇 세대인지 약관에서 먼저 확인하기
   > 2. 최근 3년 병원 방문 빈도 따져보기
   > 3. 임신·출산 계획 있다면 5세대가 유리할 수 있음
   > 4. 갈아타기 전 보험사 상담으로 시뮬레이션 받기

═══════════════════════════════════
`;

const TONE_RULES = `
**문체·톤 (카카오페이 머니콘텐츠 그대로):**

- 친근한 존댓말: "~예요", "~네요", "~있어요", "~해보세요"
- 연결어 적극 사용: "그런데", "그래서", "그동안", "예를 들어", "가령", "근데", "사실"
- 한 문장 25~40자, 길지 않게
- 전문용어는 1번 등장 시 괄호 설명: "자기부담금(본인이 내는 비율)"
- 권유 표현: "~따져보세요", "~고려해보세요", "~확인하세요"
- **수치는 신중하게**: 확실하지 않은 보험료/한도/등급 점수 등은 일반론으로. 검증 안 된 구체 수치 X.

**전체 분량**: 1,800~2,400자 (마크다운 마커 제외)
**굵게 강조 횟수**: 18~25회 (전체)
**콜아웃 총 개수**: 2~3개 (본문 1~2개 + 마무리 📌 1개)

**시각 요소 적극 활용 (본문이 단조롭지 않게 — 카카오페이는 시각 풍부함이 핵심):**

- **표 (GFM)**: 비교 가능한 정보(예: 4세대 vs 5세대, 환급률 단계, 등급별 보험금 등) 무조건 표로 정리
  | 항목 | A | B |
  |------|---|---|
  | 보험료 | ... | ... |

- **굵게 \`**...**\`**: 18~25회. 핵심 단어·수치·용어를 그냥 폰트 굵기로만 강조. 일반적인 강조용.

- **형광펜 \`<mark>...</mark>\`**: 글 전체에서 **정확히 2~3회만**. 진짜 핵심 한 문장 또는 핵심 키워드에만 사용.
  남용하면 강조가 약해지므로 정말 중요한 부분만. 굵게(\`**\`)와 다른 용도.
  예) 도입부의 핵심 결론 한 구절, 마무리 직전의 가장 중요한 한 단어.

- **밑줄 \`<u>...</u>\`**: 글 전체에서 1~2회만. 행동 권유 문장 ("○○부터 확인하세요").

- **번호 리스트 \`1.\`**: 단계·절차일 때 사용

- **체크리스트 \`-\`**: 항목 나열일 때 사용 (자동으로 ✓ 아이콘 렌더링)

- **범위 표현 주의**: "5~10배", "2억~4억" 처럼 \`~\`(물결)을 범위 표시로 쓰지 마세요. 마크다운이 취소선으로 잘못 해석합니다.
  대신: "5에서 10배", "5배에서 10배", "2억에서 4억 원" 등으로 풀어쓰거나 "5–10배"(en dash) 사용.
`;

const TEMPLATE = (topic: Topic) => `당신은 카카오페이 머니콘텐츠의 보험 가이드 작가입니다.
일반 고객이 "어, 이거 내 얘기네?" 하고 끝까지 읽게 만드는 글을 씁니다.

**주제**: "${topic.title}"
**도입 훅**: ${topic.hook}
**카테고리**: ${topic.category}
**본문에서 다룰 핵심 포인트**:
${topic.outline.map((o, i) => `${i + 1}. ${o}`).join('\n')}

${ANONYMIZATION_RULES}

${KAKAOPAY_STRUCTURE}

${TONE_RULES}

**출력 형식 (반드시 이 JSON 한 객체만, 코드블록·설명·앞뒤 텍스트 없이):**
{
  "title": "최종 제목 (질문형 권장, 후킹 강하게, 25~35자)",
  "body_md": "마크다운 본문 (도입 → ## H2 3~4개 → 📌 마무리 콜아웃 / 1,800~2,400자)"
}`;

export async function generateTopicArticle(topic: Topic): Promise<GeneratedSummary> {
  const apiKey = process.env.ANTHROPIC_API_KEY;
  if (!apiKey) throw new Error('ANTHROPIC_API_KEY missing');
  const client = new Anthropic({ apiKey });

  const message = await client.messages.create({
    model: 'claude-haiku-4-5',
    max_tokens: 8192,
    temperature: 0.85,
    messages: [{ role: 'user', content: TEMPLATE(topic) }],
  });

  const textBlock = message.content.find(b => b.type === 'text');
  if (!textBlock || textBlock.type !== 'text') {
    throw new Error('Claude 응답에 text block 없음');
  }
  const text = textBlock.text.trim();
  const s = text.indexOf('{');
  const e = text.lastIndexOf('}');
  if (s === -1 || e === -1) {
    console.error('[generator] Claude raw (no JSON):', text.slice(0, 1000));
    throw new Error('Claude 응답 JSON 미발견: ' + text.slice(0, 200));
  }
  let obj: { title?: unknown; body_md?: unknown };
  try {
    obj = JSON.parse(text.slice(s, e + 1));
  } catch (err) {
    console.error('[generator] JSON parse failed. Raw:', text.slice(0, 1000));
    throw err;
  }
  if (typeof obj.title !== 'string' || typeof obj.body_md !== 'string') {
    throw new Error('Claude 응답 형식 오류');
  }
  return { title: obj.title, body_md: obj.body_md };
}
