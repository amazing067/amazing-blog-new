# Google OAuth 클라이언트 ID 설정 가이드

## 📋 OAuth 클라이언트 ID 생성

### 1단계: 기본 설정

이미지에서 보이는 설정을 그대로 사용하세요:

- **애플리케이션 유형**: `웹 애플리케이션` ✅ (이미 선택됨)
- **이름**: `blog_ai` ✅ (이미 입력됨)

### 2단계: URI 설정

#### ✅ 승인된 JavaScript 원본
**필요 없습니다.** (Node.js 스크립트이므로 브라우저가 아닙니다)

#### ✅ 승인된 리디렉션 URI
**다음 URI를 추가하세요:**

```
http://localhost:3000
http://localhost
http://127.0.0.1:3000
```

또는 더 간단하게:
```
http://localhost
```

**설명**: OAuth 인증 후 리디렉션되는 주소입니다. 로컬 개발 환경이므로 `localhost`를 사용합니다.

### 3단계: 만들기 클릭

"만들기" 버튼을 클릭하면 클라이언트 ID와 클라이언트 시크릿이 생성됩니다.

---

## 🔑 클라이언트 ID 저장

생성된 후:
1. **클라이언트 ID** 복사
2. **클라이언트 시크릿** 복사
3. `.env.local` 파일에 추가:

```env
GOOGLE_OAUTH_CLIENT_ID=your-client-id-here
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret-here
```

---

## ⚠️ 참고사항

- 설정이 적용되는 데 **5분에서 몇 시간**이 걸릴 수 있습니다.
- OAuth 동의 화면 설정도 필요할 수 있습니다 (처음 사용하는 경우).

