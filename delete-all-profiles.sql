-- ⚠️ 경고: 이 스크립트는 profiles 테이블의 모든 데이터를 삭제합니다!
-- 실행 전에 반드시 백업을 확인하세요!
-- 
-- 실행 방법: Supabase SQL Editor에서 + New query → 전체 복사 → RUN
--
-- 이 스크립트는 다음을 수행합니다:
-- 1. profiles 테이블의 모든 데이터 삭제
-- 2. auth.users의 모든 사용자 삭제 (선택사항)

-- ============================================
-- 1. profiles 테이블의 모든 데이터 삭제
-- ============================================

DELETE FROM profiles;

-- ============================================
-- 2. auth.users의 모든 사용자 삭제 (선택사항)
-- ============================================
-- 주의: 이 부분은 Supabase Dashboard의 Authentication > Users에서
-- 수동으로 삭제하거나, 아래 주석을 해제하여 실행할 수 있습니다.
-- 
-- ⚠️ 주의: 이 쿼리는 모든 인증 사용자를 삭제합니다!
-- 
-- DELETE FROM auth.users;

-- ============================================
-- 3. 확인 쿼리
-- ============================================

-- 남아있는 프로필 수 확인
SELECT 
  COUNT(*) as remaining_profiles 
FROM 
  profiles;

-- 남아있는 사용자 수 확인 (auth.users)
SELECT 
  COUNT(*) as remaining_users 
FROM 
  auth.users;

