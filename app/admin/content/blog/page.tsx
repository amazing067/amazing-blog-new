import MovedNotice from '../_MovedNotice';

// 블로그(콘텐츠 허브) 생성·검수는 amazing-biz-server로 이전되어 이 repo에서는 비활성화되었습니다.
// ※ 회원용 수동 블로그 생성기(/dashboard)는 별개이며 그대로 유지됩니다.
export default function BlogHubPage() {
  return <MovedNotice title="블로그(자동생성) 기능은 이전되었습니다" />;
}
