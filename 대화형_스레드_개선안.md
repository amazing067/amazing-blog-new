# 대화형 스레드 개선안: 여러 사람이 댓글을 다는 것처럼

**작성일**: 2024-12-16  
**목적**: 현재는 질문자와 설계사만 번갈아가며 대화하는데, 실제 카페처럼 여러 사람이 댓글을 다는 것처럼 만들기

---

## 📊 현재 대화 구조

### 현재 방식
```
1. customer (질문자) - 첫 질문
2. agent (설계사) - 첫 답변
3. customer (질문자) - 추가 질문
4. agent (설계사) - 답변
5. customer (질문자) - 추가 질문
6. agent (설계사) - 답변
...
```

**문제점:**
- 한 사람(질문자)이 계속 질문하는 것처럼 보임
- 실제 카페에서는 여러 사람이 댓글을 다는 것처럼 보이지 않음

---

## 💡 개선안: 여러 사람이 댓글을 다는 것처럼

### 개선된 방식
```
1. customer1 (질문자) - 첫 질문
2. agent (설계사) - 첫 답변
3. customer2 (다른 사람) - "저도 궁금했어요" 같은 댓글
4. agent (설계사) - 답변
5. customer1 (질문자) - 추가 질문
6. customer3 (또 다른 사람) - "이 부분도 궁금해요" 같은 댓글
7. agent (설계사) - 답변
8. customer2 (다른 사람) - "감사합니다" 같은 댓글
...
```

**장점:**
- 실제 카페처럼 여러 사람이 참여하는 것처럼 보임
- 더 자연스럽고 생동감 있는 대화
- 설계사의 전문성이 더 돋보임 (여러 사람의 질문에 답변)

---

## 🎯 구현 방법

### 1. 고객 역할 다양화

**현재:**
- 모든 customer 댓글은 같은 질문자로 생성

**개선:**
- 여러 명의 다른 고객 역할 생성
- 각 고객은 다른 스타일, 다른 관점, 다른 질문

**고객 역할 예시:**
- `customer1` (질문자): 원래 질문을 올린 사람
- `customer2` (관심자): "저도 궁금했어요", "이 부분도 궁금해요" 같은 댓글
- `customer3` (비교자): "다른 보험사는 어때요?", "비교가 궁금해요" 같은 댓글
- `customer4` (확인자): "정말 그런가요?", "확인하고 싶어요" 같은 댓글

### 2. 대화 패턴 다양화

**패턴 1: 질문자 중심**
```
1. customer1 (질문자) - 첫 질문
2. agent (설계사) - 첫 답변
3. customer1 (질문자) - 추가 질문
4. agent (설계사) - 답변
5. customer1 (질문자) - 추가 질문
6. agent (설계사) - 답변
```

**패턴 2: 여러 사람 참여 (추천 ⭐)**
```
1. customer1 (질문자) - 첫 질문
2. agent (설계사) - 첫 답변
3. customer2 (관심자) - "저도 궁금했어요"
4. agent (설계사) - 답변
5. customer1 (질문자) - 추가 질문
6. customer3 (비교자) - "다른 보험사는 어때요?"
7. agent (설계사) - 답변
8. customer2 (관심자) - "감사합니다"
```

**패턴 3: 혼합형**
```
1. customer1 (질문자) - 첫 질문
2. agent (설계사) - 첫 답변
3. customer2 (관심자) - "저도 궁금했어요"
4. agent (설계사) - 답변
5. customer1 (질문자) - 추가 질문
6. agent (설계사) - 답변
7. customer3 (비교자) - "다른 보험사는 어때요?"
8. agent (설계사) - 답변
```

### 3. 고객 역할별 프롬프트

**customer1 (질문자):**
- 원래 질문을 올린 사람
- 추가 질문, 확인 질문, 구체적 질문

**customer2 (관심자):**
- "저도 궁금했어요"
- "이 부분도 궁금해요"
- "감사합니다"
- "도움이 되었어요"

**customer3 (비교자):**
- "다른 보험사는 어때요?"
- "비교가 궁금해요"
- "A사와 B사 중 어느 게 나을까요?"

**customer4 (확인자):**
- "정말 그런가요?"
- "확인하고 싶어요"
- "이 부분도 확인 가능한가요?"

---

## 🔧 구현 세부사항

### 1. 고객 역할 결정 로직

```typescript
// 각 스텝마다 고객 역할을 랜덤하게 결정
const customerRoles = ['customer1', 'customer2', 'customer3', 'customer4']
const getCustomerRole = (step: number, totalSteps: number) => {
  // 첫 번째 고객 댓글은 항상 customer1 (질문자)
  if (step === 3) return 'customer1'
  
  // 이후는 랜덤하게 결정 (하지만 customer1이 50% 확률로 나오도록)
  const rand = Math.random()
  if (rand < 0.5) return 'customer1' // 질문자가 계속 질문
  else if (rand < 0.7) return 'customer2' // 관심자
  else if (rand < 0.9) return 'customer3' // 비교자
  else return 'customer4' // 확인자
}
```

### 2. 프롬프트 수정

**고객 댓글 프롬프트에 역할 추가:**
```typescript
// customer1 (질문자)
"당신은 원래 질문을 올린 사람입니다. 설계사의 답변을 보고 추가 질문을 하세요."

// customer2 (관심자)
"당신은 이 질문에 관심이 있는 다른 사람입니다. '저도 궁금했어요' 같은 댓글을 작성하세요."

// customer3 (비교자)
"당신은 다른 보험사와 비교하고 싶어하는 사람입니다. '다른 보험사는 어때요?' 같은 댓글을 작성하세요."

// customer4 (확인자)
"당신은 설계사의 답변을 확인하고 싶어하는 사람입니다. '정말 그런가요?' 같은 댓글을 작성하세요."
```

### 3. 대화 흐름 예시

**예시 1: 여러 사람 참여**
```
👤 질문자: 35살 남자, 교보생명 어린이암보험 꼭 들어야 하나요?
(본문...)

👨‍💼 설계사: 👍 질문자님~ 안녕하세요 15년차 보험 전문가입니다...
📋 네 35세 남성분도 어린이보험 가입이 훨씬 유리합니다...

👤 다른분: 저도 궁금했어요 30대도 어린이보험 가입이 가능한가요?

👨‍💼 설계사: ✅ 네^^ 30대도 어린이보험 가입이 가능합니다...

👤 질문자: 암 진단비 최대 1억 5천까지 된다고 하셧는데, 그럼 유사암 진단비는 얼마까지 가능한지 궁금해요

👤 또다른분: 다른 보험사는 어때요? 비교가 궁금해요

👨‍💼 설계사: 💡 질문자님~ 정확히 말씀드리면 네, 유사암 진단비는 3천만원까지 가능합니다^^
성인보험의 유사암 진단비가 보통 1천만원에서 최대 2천만원인 점을 감안하면...
```

---

## 📋 구현 체크리스트

- [ ] 고객 역할 다양화 (customer1, customer2, customer3, customer4)
- [ ] 각 역할별 프롬프트 작성
- [ ] 대화 패턴 결정 로직 구현
- [ ] 프롬프트에 역할 정보 추가
- [ ] 테스트 및 검증

---

## 🎨 UI 표시 방법

**현재:**
```
👤 고객: ...
👨‍💼 설계사: ...
```

**개선안:**
```
👤 질문자: ...
👨‍💼 설계사: ...
👤 다른분: ...
👨‍💼 설계사: ...
👤 또다른분: ...
```

또는

```
👤 고객A: ...
👨‍💼 설계사: ...
👤 고객B: ...
👨‍💼 설계사: ...
👤 고객C: ...
```

---

**작성자**: AI Assistant  
**상태**: 제안 완료 ✅

